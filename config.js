/**
 * ============================================================
 *  КОНФИГУРАЦИЯ КАЛЬКУЛЯТОРА НАВЕСА
 *  Все цены и коэффициенты редактируются здесь.
 *  Менеджер может менять значения без правки основного кода.
 * ============================================================
 */
const CONFIG = {

  /* ── Базовые цены ────────────────────────────────────────── */
  progressivePrices: [
    { minArea: 70, priceTotal: 8500 },
    { minArea: 60, priceTotal: 10000 },
    { minArea: 50, priceTotal: 12000 },
    { minArea: 40, priceTotal: 14000 },
    { minArea: 0, priceTotal: 15300 }
  ],
  framePricePerM2: 11800,           // Базовая цена каркаса за 1 м² (fallback)
  roofPricePerM2: 1000,             // Цена кровли (профнастил) за 1 м² (включена всегда)
  drainPricePerM: 2000,             // Цена водостока за 1 пог. м

  /* ── Коэффициенты ────────────────────────────────────────── */
  heightThreshold: 2.5,       // Порог высоты (м), после которого применяется коэф.
  heightCoefficient: 1.25,      // Коэффициент удорожания при высоте > порога

  minOrderArea: 15,        // Мин. площадь заказа (м²), ниже — применяется наценка
  smallOrderCoeff: 1.4,       // Коэффициент наценки малого заказа

  roundingStep: 500,       // Шаг округления итоговой суммы (₽)

  /* ── Освещение ───────────────────────────────────────────── */
  lighting: {
    basePrice: 30000,            // Базовая цена комплекта света
    areaStep: 15,              // Каждые N м² — +stepCoeff к коэффициенту
    stepCoeff: 0.3,             // Прирост коэффициента за шаг
    maxCoeff: 2.5,             // Максимальный коэффициент
  },

  /* ── Справочник материалов стен (используется для стен, фриза и хозблока) ── */
  wallMaterials: [
    { key: 'planken', name: 'Планкен', pricePerM2: 2400, pricePerLinearM: 3000 },
    { key: 'metal_jalousie', name: 'Металл/жалюзи', pricePerM2: 3600, pricePerLinearM: 4500 },
    { key: 'wpc', name: 'ДПК', pricePerM2: 5000, pricePerLinearM: 3500 },
  ],

  /* ── Справочник материалов потолка ───────────────────────── */
  ceilingMaterials: [
    { key: 'planken', name: 'Планкен', pricePerM2: 2200 },
    { key: 'metal', name: 'Металл', pricePerM2: 2200 },
    { key: 'suspended_ceilings', name: 'Подвесные потолочные системы', pricePerM2: 7800 },
    { key: 'wpc', name: 'ДПК', pricePerM2: 6000 },
  ],

  /* ── Хозблок ─────────────────────────────────────────────── */
  hozblok: {
    doorPrice: 15000,       // Фиксированная стоимость двери (₽)
    lightingPrice: 5000,        // Фиксированная стоимость освещения (₽)
    laborCoefficient: 1.1,         // Повышающий коэффициент за трудоёмкость
  },

  /* ── Ограничения полей ввода ─────────────────────────────── */
  limits: {
    length: { min: 2, max: 20, step: 0.5, default: 6 },
    width: { min: 2, max: 10, step: 0.5, default: 3.5 },
    height: { min: 2, max: 4.5, step: 0.1, default: 2.5 },
    hozblokLength: { min: 2, max: 20, step: 0.5, default: 3 },
    hozblokWidth: { min: 2, max: 10, step: 0.5, default: 2 },
  },
};
